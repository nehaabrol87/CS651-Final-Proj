export class LoginController {

  public loginRequestObject;
  public currentYear = (new Date()).getFullYear();


  constructor() {
   // this.user = new User();
   // this.loginRequestObject = this.userService.loginRequestObject;
  }

  public login() {
    //.userService.login(this.user);
  }

  openSupportDialog() {
   // this.popupService.openSupportDialog();
  }
}
